const totalController = require("./totalController");
const AuthController = require("./AuthController");
const RegisterTransactionController = require("./RegisterTransactionController");
const ReferralController = require("./ReferralController");

module.exports = {
  totalController,
  AuthController,
  RegisterTransactionController,
  ReferralController,
};
